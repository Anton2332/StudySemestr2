#include<conio.h>
#include<iostream>
#include<stralign.h>
#include<stdlib.h>



using namespace std;

class fotop
{
	string model,mater;
	int zoom;
	float vart;
public:
	fotop()
	{

	}



	fotop(int k)
	{
		cout << "??????? ?????? : ";
		cin >> model;
		cout << "??????? ?????????? (?????? 1 ? ????? 35) : ";
		while (zoom < 1)
		{
			cin >> zoom;
			if (zoom < 1 || zoom>35)
			{
				cout << "??????? ?? ???????? ???????? !!!" << endl;
				cout << "??????? ?????????? (?????? 1 ? ????? 35) : ";
			}
			else
				break;
		}
		cout << "??????? ???????? (metal or plastic)???(????? ??? ???????) : ";
		while (0 < 1)
		{
			cin >> mater;
			if (mater == "metal"||mater=="plastic"||mater=="?????"||mater=="???????"||mater == "Metal" || mater == "Plastic" || mater == "?????" || mater == "???????")
				break;
			else
			{
				cout << "??????? ?? ???????? ???????? !!!" << endl;
				cout << "??????? ???????? (metal or plastic)???(????? ??? ???????) : ";
			}
		}

	}
	
	void vartfunc()
	{
		if (mater == "metal" || mater == "?????" || mater == "Metal" || mater == "?????")
			vart = (zoom + 2) * 15;
		else
			vart = (zoom + 2) * 10;

	}

	void inform()
	{
		cout << "?????? : " << model << " Zoom : " << zoom << " ???????? : " << vart <<" $"<< endl;
	}

	bool dorog()
	{
		if (vart > 200)
		{
			return true;
		}
		else
			return false;
	}

};

class tsifr : public fotop
{
	int kilmeg;
public :
	tsifr()
	{

	}


	tsifr(int k)
	{
		fotop(1);
		vartfunc();

		cout << "??????? ????????? ???????????? : ";
		cin >> kilmeg;
	}

	void novmod()
	{
		kilmeg = kilmeg + 2;
	}

};





int main()
{
	setlocale(LC_CTYPE, "ukr");


	return 0;
}
